info:
  name: End Session
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{base_url}}/session/{{session_id}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
