info:
  name: Clear History
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{base_url}}/session/{{session_id}}/history"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
